import { DisableForEmpDirective } from './disable-for-emp.directive';

describe('DisableForEmpDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableForEmpDirective();
    expect(directive).toBeTruthy();
  });
});
